const INNITIAL_STATE ={
  nome: '',
  email: '',
  avatarURL: '',
};


function user (state = INNITIAL_STATE, action) {
  switch(action.type) {
    default:
      return state;
  }
};

export default user;