import React from 'react';
import Header from '../components/quiz/Header';

class Quiz extends React.Component {
  constructor(props) {
    super(props);
    this.state = { };
  }

  render() {
    return (
      <Header />
    );
  }
}

export default Quiz;
